[project]
name = "fusion-equilibrium-challenge-starter"
version = "0.1.0"
description = "Starter kit for the Fusion Equilibrium Challenge (NeurIPS hackathon)"
readme = "README.md"
requires-python = ">=3.10,<3.13"
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "pyarrow>=14.0",
    "scipy>=1.11",
    "scikit-learn>=1.3",
    "matplotlib>=3.7",
    "scikit-image>=0.22",
    "datasets>=2.14",
    "plotly>=5.18",
]

[dependency-groups]
pytorch = [
    "torch>=2.0,<2.6",
]

[tool.pyright]
# Где искать сторонние пакеты: статический анализатор не исполняет код и сам venv не найдёт.
venvPath = "."
venv = ".venv"
# Корень проекта в путях поиска — это то, что my_experiments/*.py делают в рантайме
# через sys.path.insert; анализатору ту строку надо продублировать декларативно.
extraPaths = ["."]

[tool.uv]
package = false

# Resolve lockfile for the platforms hackathoners typically use.
required-environments = [
    "sys_platform == 'linux' and platform_machine == 'x86_64'",
    "sys_platform == 'darwin' and platform_machine == 'arm64'",
    "sys_platform == 'win32' and platform_machine == 'AMD64'",
]
